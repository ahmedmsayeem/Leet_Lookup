{
  "name": "leetlookup",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed for Bun'",
    "start": "bun run server.ts",
    "dev": "bun run server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.4",
    "express": "^5.1.0"
  }
}
